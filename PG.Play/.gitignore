node_modules
.DS_Store
*.log
.vite

# NOTE: dist/ is intentionally NOT ignored — GitHub Pages serves the
# arcade from the repo's dist/ subdirectory at pushkalgupta.com/PG.Play/dist/.
# If you ever swap to a separate gh-pages branch, re-add `dist` here.

# environment
.env
.env.local
.env.*.local

# supabase
.branches
.temp

# generated cover variants — regenerate via `npm run optimize:covers`
public/covers/*-640.webp
public/covers/*-1280.webp
public/covers/*-1920.webp
public/covers/*-1280.jpg
public/og.webp
public/og-1200.jpg
