// Thin re-export. The full Goalbound experience lives in
// ./goalbound/ as a self-contained multi-screen app with its own
// routing, persistence, and gameplay engine.

export { default } from './goalbound/index.jsx';
