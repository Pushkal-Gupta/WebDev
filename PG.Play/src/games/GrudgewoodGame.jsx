// Grudgewood — full 3D rage-comedy forest game.
// The original 2D side-scroller has been replaced with a true 3D Three.js
// build. This file is now a thin re-export so the platform's lazy import
// continues to resolve to a default React component.
//
// Source lives in ./grudgewood/ — see index.jsx for the entry point and the
// per-module headers for architecture notes.

export { default } from './grudgewood/index.jsx';
