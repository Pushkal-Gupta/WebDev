package com.pghub.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// PG Hub dark palette — mirrors the web app's design tokens.
val PgBg = Color(0xFF0A1615)
val PgSurface = Color(0xFF0F1E1D)
val PgSurfaceElevated = Color(0xFF122927)
val PgText = Color(0xFFFFFFFF)
val PgDim = Color(0xFF6F9B98)
val PgAccent = Color(0xFF00FFF5)
val PgBorder = Color(0xFF17423E)

// Difficulty accents (kept distinct from the primary cyan for scannability).
val DifficultyEasy = Color(0xFF3DE8A4)
val DifficultyMedium = Color(0xFFFFC24B)
val DifficultyHard = Color(0xFFFF5C7A)
